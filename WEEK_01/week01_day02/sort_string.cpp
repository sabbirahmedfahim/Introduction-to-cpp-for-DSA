#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    char arr[n];
    cin>>arr;
    // sort(arr, arr+n);
    
    for (int i = 0; i < n; i++)
    {
        cout<<arr[i];
    }
    return 0;
}