#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    string name;

    cin>>n;
    getline(cin, name);

    cout<<n<<endl<<name; //the output will just an integer.
    //Quiz: did u noticec the error? 
    // if u do not get it, don't worry, Goto fix_getline_3

    return 0;
}