#include <bits/stdc++.h>
using namespace std;

int main()
{ //switching first index : i prefer name[0]
    string name = "Sabbir";
    cout << name[0] << endl;    // using index
    cout << name.at(0) << endl; // using at_function
    //both are doing same job

    return 0;
}