#include <bits/stdc++.h>
using namespace std;

int main() //pop_back: remove the last character of the string.

{
    string name = "FahimM";
    name.pop_back();
    cout<<name<<endl;

    name.pop_back();
    cout<<name<<endl;

    return 0;
}